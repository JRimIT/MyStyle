<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Profile - My Style</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/css/navbar.css" />
    <link rel="stylesheet" href="/css/profile.css" />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"
    />
  </head>
  <body>
    <%- include('../partials/navbar') %>

    <div class="container mt-5 pt-5 profile-container">
      <div class="card shadow p-4">
        <div class="text-center">
          <img
            src="<%= user.avatarUrl || 'https://cdn-icons-png.flaticon.com/512/149/149071.png' %>"
            class="rounded-circle mb-3"
            width="120"
            alt="Avatar"
          />
          <h3><%= user.fullName || user.username %></h3>
          <p><i class="fas fa-envelope me-1"></i> <%= user.email || "..." %></p>
        </div>
        <hr />
        <ul class="list-group list-group-flush">
          <li class="list-group-item">
            <strong>Username:</strong> <%= user.username %>
          </li>
          <li class="list-group-item">
            <strong>Phone:</strong> <%= user.phone || '...' %>
          </li>
          <li class="list-group-item">
            <strong>Address:</strong> <%= user.address || '...' %>
          </li>
          <li class="list-group-item">
            <strong>Post Code:</strong> <%= user.postCode || '...' %>
          </li>
          <li class="list-group-item">
            <strong>Date of Birth:</strong>
            <%= user.dateOfBirth ? user.dateOfBirth.toDateString() : '...' %>
          </li>

          <li class="list-group-item">
            <strong>Role:</strong> <%= user.role %>
          </li>
          <li class="list-group-item">
            <strong>Joined:</strong> <%= user.createdAt ?
            user.createdAt.toDateString() : 'Not provided' %>
          </li>
          <li class="list-group-item">
            <strong>Số dư ví:</strong> <%= user.balance ?
            user.balance.toLocaleString() : 0 %> VND
          </li>
        </ul>
      </div>
    </div>

    <%- include('../partials/footer') %>
  </body>
</html>
